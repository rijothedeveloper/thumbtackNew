import Logo from "../../assets/logo.svg";

const Navbar = () => {
    return (
        <section className="navbar">
            <img src={Logo} alt="logo"/>
        </section>
    )
}

export default Navbar;